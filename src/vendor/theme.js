import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#242424",
    },
    secondary: {
      main: "#fff",
    },
  },
  typography: {
    fontFamily: "CabinetGrotesk, Arial, sans-serif",
  },
});

export default theme;
